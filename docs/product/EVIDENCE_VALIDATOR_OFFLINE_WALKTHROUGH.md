# 证据校验器离线走查

更新时间：2026-09-19
性质：人工离线走查，不是程序已实现结果
数据范围：T001 v1、v2、v3 三条合成任务的既有输出，不调用模型

## 1. 走查目的

验证《独立证据校验器产品规格》中的规则是否覆盖已经出现的 Badcase。这里只证明规则设计能够描述和拦截已知问题，不证明自动校验器已经开发完成，也不代表对未知样本有效。

## 2. 走查结果

| 样本 | 已知问题 | 命中规则 | 级别 | 期望状态 | 人工结论 |
|---|---|---|---|---|---|
| T001 v1 | 历史发布时模型被写成当前事实 | EV-01 | 阻断 | blocked | 命中 |
| T001 v1 | “未找到量化指标”与同页 26.6% 反例冲突 | EV-03 | 阻断 | blocked | 命中 |
| T001 v1 | 遗漏幻觉、错误推断等质量限制 | EV-04 | 阻断 | blocked | 命中 |
| T001 v1 | 正文超过范围 | EV-06 | 阻断 | blocked | 命中 |
| T001 v2 | 遗漏模型质量限制 | EV-04 | 阻断 | blocked | 命中 |
| T001 v2 | 待复核项暗含无来源的“Pro $200 暂停注册” | EV-08 | 阻断 | blocked | 命中 |
| T001 v3 | 已查看 limitations，却写成“未披露” | EV-03 | 阻断 | blocked | 命中 |
| T001 v3 | 页面可见正文 877 个字符，自报约 560 | EV-06 | 阻断 | blocked | 命中 |
| T001 v3 | URL 锚点和帮助页别名导致页面统计含混 | EV-07 | 警告 | blocked | 命中；阻断状态由 EV-03、EV-06 决定 |

## 3. 结果汇总

- 已知阻断问题：8 项。
- 规则走查命中：8 项。
- 已知问题规则覆盖率：100%。
- 三条样本期望发布状态：全部 `blocked`。
- 新增模型调用：0 次。
- 新增 API 费用：0 元。

以上数字只是人工把已知问题映射到规则的结果，不能称为自动召回率、真实准确率或生产效果。

## 4. 发现的设计缺口

1. EV-03 需要保存页面原文摘录，否则无法执行否定性结论反查。
2. EV-04 需要 ResearchBrief 先声明必填维度，不能在报告生成后临时决定。
3. EV-06 必须基于最终展示文本计算，不能使用模型自报数字。
4. EV-07 需要定义 URL 规范化口径：去锚点、去跟踪参数、识别重定向和别名。
5. 仅靠规则不能判断所有语义支持关系；无法确定时必须进入人工复核。

## 5. 进入实现前的通过条件

- 产品负责人确认首版规则和严重级别。
- 确认 `ResearchBrief`、`Claim`、`EvidenceItem`、`ValidationFinding` 的最小字段。
- 确认 fail-closed：校验器异常时不得绕过。
- 确认首版只做离线确定性规则，不新增模型调用。

满足上述条件后，才进入阶段 A 原型；本走查本身不等于功能完成。
